# Copy this file to .env and edit values before running with docker-compose
# Example settings
PORT=5001
MONGODB_URI=mongodb://mongo:27017/stt-database

# Placeholders for API keys used by the project (do NOT commit real keys)
OPENAI_API_KEY=
GEMINI_API_KEY=
